# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.
- [2026-09-08] lib/server/tournaments/state.ts:167-169 (branche RUNNING de `syncTournamentState`) — l'entretien passif tranche un report de score expiré (`resolveExpiredScoreReports`) mais n'appelle ensuite ni `reconcileSwiss`/`reconcileSurvival`/`reconcileEndurance` ni `reconcilePhases` : si la manche ainsi tranchée est la dernière de sa ronde, aucune manche suivante n'est posée et le tournoi reste « en cours » à jamais (il sort même de `findDueMaintenance`, plus rien ne le revisite). Seuls `SINGLE`/`DOUBLE` s'en sortent, `finalizeMatch` propageant par les liens de bracket. — Piste : rejouer dans cette branche la même chaîne de réconciliation que `reportMatchScorePublic` (index.ts:918-933), sous condition qu'un report ait effectivement été tranché pour ne pas rejouer tout le tournoi à chaque balayage. — (rencontré sur : review tâche 22 / feature/review-task-22-6d3335)
- [2026-09-08] lib/server/tournaments/bracket-generator.ts:110 — `publishUpdatedEvent` est appelé **dans la transaction ouverte** (`createBracketIfMissing` tourne sous `syncTournamentState`), seul point du projet à le faire : l'invalidation et l'événement partent avant le commit, la salle SSE se réveille aussitôt, reconstruit l'instantané sur une autre connexion — qui ne voit pas encore le plateau — et met en cache puis diffuse un tournoi « en cours sans plateau ». Aucun second événement ne suit (l'état n'a pas changé), la trame périmée tient donc jusqu'au battement d'entretien de la salle (30 s). Un rollback diffuserait par ailleurs un plateau qui n'a jamais existé. — Piste : rendre l'information à l'appelant (comme `{ finished }`) et publier après `commit()`, à l'image de tous les autres chemins d'écriture. — (rencontré sur : review tâche 22 / feature/review-task-22-6d3335)
- [2026-09-08] lib/server/tournaments/snapshot.ts:113-115 (`loadMaintainedRow`) — la condition `bracket_size IS NULL` sert de test « plateau d'élimination manquant » mais n'est pas gardée par le format : en `MULTI`, `bg_tournaments.bracket_size` n'est **jamais** renseigné (les plateaux de phase écrivent sur `bg_tournament_phases`, bracket-single.ts:111-115), si bien que chaque reconstruction d'instantané d'un multi-phases en cours ouvre une transaction d'entretien. Le module promet pourtant qu'« une lecture ordinaire ne coûte rien de plus qu'un SELECT ». `findDueMaintenance` (sync-scope.ts:81) restreint bien la même condition à `SINGLE`/`DOUBLE`. — Piste : appliquer le même filtre de format ici. — (rencontré sur : review tâche 22 / feature/review-task-22-6d3335)
- [2026-09-08] app/api/bot/feed/stream/route.ts — flux SSE **non authentifié**, sans plafond de débit ni plafond de flux simultanés, qui ouvre une connexion sortante vers le bot par client : les garde-fous posés sur le flux de tournoi (`getCurrentUser` + `STREAM_OPEN_RULE` + `acquireStreamSlot`) n'ont jamais été étendus à cette route, publique via `/bot`. Idem pour `/api/bot/activity` et `/api/bot/stats`. — Piste : reprendre le trio de gardes du flux de tournoi, ou mutualiser un unique abonnement amont pour tous les lecteurs. — (rencontré sur : review tâche 22 / feature/review-task-22-6d3335)
- [2026-09-08] components/bot/BotLiveFeed.tsx:11-35 — l'effet qui ouvre l'`EventSource` dépend de `paused` : mettre le flux en pause (ou le reprendre) **ferme et rouvre** la connexion SSE, donc aussi la connexion sortante vers le bot, et fait perdre les évènements de l'intervalle. — Piste : lire `paused` dans une ref, l'effet ne dépendant que du montage. — (rencontré sur : review tâche 22 / feature/review-task-22-6d3335)

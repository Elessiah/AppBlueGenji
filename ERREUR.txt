# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.


- [2026-09-09] components/recruitment-highlight.tsx — sur mobile, la modale de recrutement est l'élément LCP de l'accueil avec 3,8 s de « render delay » (LCP 4,4 s, performance 84) : elle n'est peinte qu'après l'hydratation puis un aller-retour vers `/api/recruitment/highlight` ; sans elle, le LCP retomberait au FCP (1,6 s) — piste d'origine : passer l'annonce en prop depuis le rendu serveur et n'en décider l'affichage qu'ensuite. **Mesuré depuis, et la piste ne suffit pas** : l'aller-retour ne pèse que ~0,2 à 0,3 s des 3,8 s (la réponse est minuscule et `getHighlightedAd` est déjà en cache à vol unique), tout le reste étant l'hydratation elle-même. Tant que la modale est peinte **par du JavaScript**, elle est peinte tard, et elle est le LCP. La seule issue est de la rendre dans le **HTML initial**, ce qui suppose que le serveur sache qui l'a déjà écartée — donc de déplacer la marque de `localStorage` vers un **cookie** (et de l'ajouter à la page RGPD), et de rendre la mise en page racine dynamique. À arbitrer avec le basculement de la CSP, qui est le même levier. Dormant en attendant : la production répond `{"ad":null}`, aucune annonce n'est marquée urgente — le défaut revient le jour où l'une le sera. La piste d'origine reste bonne en soi (elle retire un aller-retour sur **chaque page** du site, presque toujours pour rien), simplement elle ne règle pas cette entrée — (rencontré sur : feature/pagespeed-audit-fixes ; remesuré sur : fix/landing-lcp-and-target-size)
- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) : **non corrigé à dessein**, la page porte l'en-tête de session (pseudo et avatar du connecté) et un retour arrière restituant la page d'un autre compte serait pire que le gain ; noté pour qu'on ne le « répare » pas par distraction — (rencontré sur : feature/pagespeed-audit-fixes)

- [2026-09-09] lib/server/api-guard.ts:requestClientIp — le repli sur l'en-tête `x-real-ip` quand `X-Forwarded-For` est absent **affaiblit** les plafonds par IP au lieu de les servir : il ne se déclenche que là où aucun relais de confiance n'a écrit `X-Forwarded-For`, donc précisément là où l'en-tête est forgeable par le client, qui obtient alors un seau neuf à chaque requête (vitrine, visites, relais de logo, flux du bot) ; dans un déploiement correctement mandaté il est mort, `X-Forwarded-For` étant toujours présent — **non corrigé à dessein** : le retirer désarmerait un déploiement qui ne poserait que `X-Real-IP`, choix qui appartient à l'exploitant, et le module documente déjà « on préfère ne pas compter que compter faux » ; côté code de connexion Discord, la demande porte sur le seul identifiant visé et n'en dépend donc pas, mais la **vérification** est depuis passée sur le couple (compte visé, IP appelante) pour cesser de punir la victime : un appelant qui forge l'en-tête obtient là un seau neuf à chaque essai — sans gain pour lui, le quota d'essais qui tient réellement le secret vivant en base, sur la ligne du code — (rencontré sur : feature/authorization-audit-fixes / PR #116)

- [2026-09-16] déploiement (nginx devant `next start`) — `www.bluegenji-esport.fr` est un piège armé : le HSTS annonce `includeSubDomains`, donc tout navigateur ayant vu l'apex en HTTPS forcera aussi le HTTPS sur `www`, or il n'existe aucun bloc `server` en 443 pour `www`, aucun `default_server` en 443, et le certificat ne porte que `DNS:bluegenji-esport.fr` — l'erreur de certificat serait alors incontournable, le HSTS retirant le « continuer quand même », et la redirection `www` du port 80 ne s'exécuterait jamais (le navigateur n'émet plus de requête en clair) ; elle renvoie d'ailleurs vers `http://` et non `https://`. Sans effet aujourd'hui : `www` ne résout pas. Piste : étendre le certificat à `www` et poser un bloc 443 qui redirige vers l'apex, **avant** d'ajouter l'enregistrement DNS — la configuration nginx n'est pas dans ce dépôt. **Le piège n'est plus inerte — il s'est armé le 2026-09-16.** L'enregistrement `www A 90.29.62.237` existe désormais dans la zone (il n'y était pas quelques heures plus tôt : `www` répondait NODATA), si bien que le scénario décrit ci-dessus n'est plus théorique et se vérifie : `https://www.bluegenji-esport.fr/` rend une **erreur de certificat** (`SEC_E_WRONG_PRINCIPAL`), le certificat servi pour ce SNI ne portant toujours que `DNS:bluegenji-esport.fr` (expire le 2026-12-01). Tout navigateur ayant vu l'apex depuis un an force le HTTPS sur `www` par le HSTS de l'apex (`max-age=31536000; includeSubDomains`, vérifié) et **n'émet donc jamais** la requête en clair que la redirection du port 80 attendait — laquelle renvoie de surcroît `Location: http://bluegenji-esport.fr/` là où celle de l'apex renvoie `https://`. Un visiteur neuf, lui, passe encore (80 → 301 → 301 → HTTPS apex). Correction : **le certificat d'abord, et c'est maintenant faisable en HTTP-01** puisque `www` résout — l'ordre ne se mord plus la queue et le détour par DNS-01 n'est plus nécessaire ; chemin ACME vérifié joignable sur `www` (301 vers l'apex, que Let's Encrypt suit). Repli immédiat si besoin : **supprimer l'enregistrement A**, ce qui rend le piège inerte à nouveau — (rencontré sur : feature/proxy-google-avatars ; armé et revérifié sur : fix/landing-lcp-and-target-size)

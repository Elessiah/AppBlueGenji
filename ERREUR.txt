# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-01] app/(secured)/joueurs/[id]/page.tsx:104, app/(secured)/profil/page.tsx:209, components/arena-nav.tsx:72, components/cyber/landing/PublicHeader.tsx:57 — avatar de repli cassé (404) pour tout compte sans avatar — `/vercel.svg` est référencé comme `src` de repli mais le fichier n'existe pas dans `public/` (reste du template Next.js, supprimé depuis) ; remplacer par un repli existant ou par la pastille à initiale déjà utilisée dans les cartes d'annuaire — (rencontré sur : feature/clickable-teams-players-9ae592)
- [2026-09-01] app/(secured)/tournois/[id]/page.tsx (rendu des matchs d'EnduranceView) — les matchs d'un tournoi BG_SURVIE sont rendus avec `format="SURVIVAL"` en dur ; sans effet aujourd'hui (`dependentMatches` range les deux formats dans la même branche) mais le verrouillage d'un score deviendrait faux le jour où les deux modes divergeront — passer `detail.card.format` (ou `"BG_SURVIE"`) — (rencontré sur : feature/bg-survie-forfeit-endurance)
- [2026-09-01] lib/server/tournaments/bg-survie.ts (reconcileEndurance, bascule en play-offs) — les play-offs peuvent démarrer **au milieu d'une manche** : le test `qualificationComplete(active.length)` précède toute vérification d'achèvement de la manche courante, si bien qu'un score reporté qui fait passer l'effectif actif sous `endurance_playoff_size` lance l'arbre alors que d'autres matchs de la manche sont encore `READY` — ils restent ouverts à jamais, `reconcileEndurance` sortant ensuite par la branche `playoffsStarted` — préexistant (même comportement avec l'ancien barème ±1) — piste : n'appeler `startEndurancePlayoffs` que si `roundIsComplete(currentRound)` — (rencontré sur : feature/bg-survie-forfeit-endurance)
- [2026-09-01] lib/shared/tournament-rules.ts + app/regles/[slug]/page.tsx (RuleCard) — les textes du registre des règles emploient la mise en gras Markdown (`**…**`, 23 occurrences) mais les pages `/regles` rendent `{paragraph}` / `{bullet}` en texte brut : les astérisques s'affichent telles quelles au visiteur — soit rendre l'emphase (mini-parser inline, comme `lib/server/bot-docs.ts`), soit retirer les marqueurs du registre — (rencontré sur : feature/bg-survie-forfeit-rules)
- [2026-09-02] components/cyber/landing/LiveCard.tsx (lignes « FR · SEED 1 » / « FR · SEED 4 » et bloc « CARTE EN COURS ») — la carte du tournoi en cours de l'accueil affiche un seed et une carte de jeu **écrits en dur**, identiques pour tous les matchs : le visiteur lit une donnée inventée, de la même famille que le « BO3 » deviné corrigé ici — piste : `LandingLive` ne transporte ni inscriptions ni instantané — il faut donc ajouter les deux seeds à `LandingLiveMatch` et joindre `bg_tournament_registrations` dans `loadLandingLive` (`lib/server/landing-service.ts`), puis retirer le bloc « carte en cours » tant que le modèle ne porte pas la map jouée — (rencontré sur : feature/match-format-labels)
- [2026-09-02] app/api/landing/live (via lib/server/landing-service.ts → listTournamentBuckets) — sur une base fraîchement seedée (76 tournois, dont 46 en cours), `GET /api/landing/live` répond en 160 s puis systématiquement en ~300 s ; l'accueil reste sur sa dernière valeur et toute écriture concurrente sur `bg_tournaments` (même un simple UPDATE) attend derrière — piste : `listTournamentBuckets` déclenche `syncTournamentState` pour chaque tournoi visible dans la requête de rendu, et la réconciliation de chaque format en cours (Suisse, Survie, BG Survie, multi-phases) s'exécute en série sous transaction ; sortir la synchronisation du chemin de lecture, ou la borner aux tournois dont un jalon est réellement franchi — non reproduit en production, observé en `next dev` juste après `npm run seed` — (rencontré sur : feature/match-format-labels)

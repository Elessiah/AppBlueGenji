# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) — **non corrigé à dessein, prémisse revérifiée le 2026-09-16** : aucune ligne du dépôt ne pose ce `no-store`, c'est le défaut de Next pour une page rendue à la demande, et toutes le sont désormais (la mise en page racine déclare sa dépendance au nonce, voir `lib/shared/csp.ts`). Le rétablir demanderait d'écraser cet en-tête dans le middleware, ce qui rendrait la page restituable par le retour arrière **après une déconnexion** — pseudo et avatar du compte précédent réaffichés sur un poste partagé. La parade existe (`Clear-Site-Data: "cache"` à la déconnexion) mais son support varie selon les navigateurs, et on échangerait une propriété de confidentialité tenue contre un point d'audit. L'entrée reste donc pour qu'on ne le « répare » pas par distraction — c'est sa seule raison d'être — (rencontré sur : feature/pagespeed-audit-fixes ; prémisse revérifiée sur : fix/erreur-txt-cleanup)

- [2026-09-21] `.github/workflows/ci.yml` (job E2E) + `app/layout.tsx` — le job E2E échoue par intermittence avant d'avoir joué le moindre test : `next dev` n'arrive pas à résoudre `@vercel/turbopack-next/internal/font/google/font` (« next/font/google queries have exactly one entry ») et Playwright abandonne sur `Timed out waiting 120000ms from config.webServer` — cause supposée : `next/font/google` va **chercher les fichiers de police chez Google au démarrage du serveur**, si bien que le job dépend d'un appel sortant vers `fonts.gstatic.com` qu'aucun cache ne couvre ; une relance du seul job passe sans changer une ligne. Piste : héberger les polices (`next/font/local`, fichiers versionnés) — c'est aussi ce que la règle « jamais une origine étrangère » demande déjà pour les images. Hors périmètre de la tâche en cours — (rencontré sur : feature/tournament-blizzard-filters)

- [2026-09-22] `components/bot/BotStatusStrip.tsx:~40` + `components/bot/BotHero.tsx:~37` — trois chiffres inventés subsistent sur `/bot` après le tri : « 99.97 % · 90 derniers jours » sous l'uptime (aucune mesure de disponibilité n'existe, ni côté site ni côté bot), « @BlueGenji Bot#8242 » dans le héros (discriminant Discord d'ailleurs supprimé par Discord depuis 2023) et le « wizard de setup » promis par `BotInviteCard`, qui est encore une case à cocher de `docs/features/BOT_FEATURES_NEEDED.md` — cause : reste de la maquette d'origine, même famille que le « BUILD 4f8a » et la liste de commandes retirés par `feature/bot-page-cleanup`. Ils ne sont pas corrigés ici parce que la tâche portait sur trois décisions nommées (section « Modules », liste de commandes, libellés d'état) et qu'aucune valeur vraie n'existe pour les remplacer : il faut d'abord décider si le bot doit publier une disponibilité, puis afficher la sienne ou retirer la case — (rencontré sur : feature/bot-page-cleanup)

- [2026-09-22] `app/bot/bot.css` (26 usages), `app/bot/docs/docs.css`, `components/bot/BotInviteCard.tsx` — `font-family: var(--mono)` ne rend **aucune** police à chasse fixe : le jeton s'appelle `--font-mono` dans `app/globals.css`, `--mono` n'est défini nulle part, et une déclaration dont la variable est absente est invalide au calcul, donc héritée — tout le « mono » de `/bot` et de `/bot/docs` (en-têtes de tableau, horodatages du flux, libellés d'état, fil d'ariane) s'affiche en Inter. Panne muette par excellence : rien ne casse, le texte s'affiche, seul le dessin diffère de l'intention. **Même défaut pour `var(--display)`** (`.srv-sigil`, une seule occurrence) : le jeton n'existe pas davantage, les initiales du sigil s'affichent donc en Inter au lieu de l'Orbitron voulue. **Et pour `var(--fg-dim)`**, jeton de couleur absent lui aussi (`app/globals.css` expose `--ink-dim`), en quatre endroits — `components/bot/BotHero.tsx:41` et `:72`, `components/bot/BotInviteCard.tsx:74`, `app/bot/docs/[[...slug]]/page.tsx:86` : la déclaration étant invalide au calcul, ces sur-titres **héritent** la couleur de leur parent au lieu du gris atténué voulu, donc s'affichent plus clairs que prévu sans qu'aucune erreur ne le dise. Correctif : remplacer `var(--mono)` par `var(--font-mono)` dans ces trois fichiers, `var(--fg-dim)` par `var(--ink-dim)`, et `var(--display)` par la famille d'affichage réellement exposée — mais c'est un changement **visuel** sur toute la page `/bot`, qui déborde du tri demandé (trois décisions nommées) et doit être vu avant d'être posé — (rencontré sur : feature/bot-page-cleanup)

- [2026-09-23] `components/bot/BotActivityChart.tsx:~24` (`useEffect` de changement de plage) — le rechargement d'une plage n'a **ni annulation ni garde d'ordre** : cliquer « 90j » puis « 7j » assez vite laisse la réponse la plus lente écraser la plus récente, si bien que le graphe, l'axe des ordonnées et « MOY. N / JOUR » décrivent 90 jours pendant que la pastille active annonce « 7j » — et rien ne le signale, les deux réponses étant valides. Cause : `fetchData()` appelle `setData(result)` sans vérifier que l'effet qui l'a lancé est toujours le courant. Piste : un `AbortController` sur le `fetch`, ou un drapeau `ignored` posé dans la fonction de nettoyage de l'effet (`return () => { ignored = true }`), le second suffisant puisque seul le `setData` est à écarter. **Préexistant** : le bloc est identique sur `main`, la branche n'a touché qu'à la dérivation des séries plus bas — hors périmètre d'une tâche de ménage portant sur trois décisions nommées (section « Modules », liste de commandes, libellés d'état) — (rencontré sur : feature/bot-page-cleanup)

- [2026-09-23] `/bot` — `components/bot/BotLatencyCard.tsx:21-23` et `components/bot/BotActivityChart.tsx:113` — **un zéro y veut encore dire deux choses**, dans les deux sens opposés. La carte de santé écrit `gateway === 0 ? "—"` : un bot au repos qui mesure réellement `cpuUsage: 0` affiche « — » pendant que sa barre rend 0 %, les deux moitiés de la cellule se contredisant. Le graphe fait l'inverse avec `botPayloadNumber(data.avgPerDay) ?? 0`, qui annonce « MOY. 0 / JOUR » — une moyenne mesurée — sur une charge que la page vient de juger illisible. C'est la classe corrigée sur `BotServersTable` par `feature/bot-page-cleanup` (repli sur « — », jamais sur zéro, le zéro restant réservé à un zéro reçu) ; `botPayloadNumber` distingue déjà les deux cas (`null` = illisible, `0` = zéro reçu) et l'information est jetée une ligne avant d'être utile. **Non corrigé ici parce que préexistant** : les deux expressions sont identiques sur `main` (la branche n'y a ajouté que le durcissement de type et la borne des barres), et la tâche portait sur trois décisions nommées — section « Modules », liste de commandes, libellés d'état. Correctif : garder le `null` de `botPayloadNumber` jusqu'à l'affichage (`?.toFixed(1) ?? "—"`) et ne poser le repli à `0` que pour la largeur de barre — (rencontré sur : feature/bot-page-cleanup)

- [2026-09-23] `tsconfig.json` (`exclude: ["tests"]`, `isolatedModules: true`) + `jest.config.js` — **aucun fichier de test n'est type-vérifié** : `npx tsc --noEmit` exclut `tests/`, et `isolatedModules` met ts-jest en transpilation seule, si bien qu'un `const x: number = "nope"` dans un test passe au vert (vérifié). Conséquence : les contrats qui ne s'expriment qu'au type — un `@ts-expect-error`, un `Parameters<typeof X>` — sont **muets** dans un test, et le seul contrôle possible reste le motif sur la source (cas de `tests/app/checkbox-style.test.ts`, clause `Omit` de `Coche`). Piste : retirer `tests` de l'`exclude` (ou ajouter un `tsconfig.tests.json` au CI) et lancer `tsc --noEmit` dessus ; s'attendre à des erreurs préexistantes à éponger. Hors périmètre de la tâche en cours — (rencontré sur : feature/styled-checkboxes)

- [2026-09-23] `app/rgpd/page.tsx:215` — la page publique RGPD rend « le temps d'une connexionpar Google, Discord ou Blizzard », sans espace — cause : le `</strong>` termine sa ligne et le texte qui le suit commence à la ligne suivante ; Babel supprime le saut de ligne en tête d'un nœud de texte JSX ainsi que l'indentation de la ligne suivante, si bien qu'aucune espace ne survit entre les deux enfants (le nettoyage n'insère une espace qu'**à l'intérieur** d'un même nœud de texte réparti sur plusieurs lignes, jamais à une frontière d'élément). Le même défaut a été corrigé au paragraphe voisin, introduit par la branche, avec `{" "}` en fin de ligne. Celui-ci **préexiste sur `main`** et vit dans la section « cookies », hors périmètre de la suppression de compte. Piste : `{" "}` après `</strong>`, ou remonter `par Google` sur la ligne du tag — (rencontré sur : feature/deleted-accounts, PR #142)

- [2026-09-23] `tests/app/bot-page-cleanup.test.ts:173` (« rend sa place au nom du serveur sur les écrans les plus étroits ») — échoue déjà sur `main`, sans rapport avec cette tâche : `css.match(/@media \(max-width: 440px\) \{[\s\S]*?\n\}\n/)` rend `null`, la regex attendant une accolade fermante suivie d'exactement un saut de ligne dans `app/bot/bot.css`, que le bloc `@media (max-width: 440px)` actuel ne respecte plus (mise en forme du fichier différente de celle supposée par le test). Vérifié en isolant l'arbre avant modification (`git stash`) : même échec. Hors périmètre de la restriction de la doc staff — (rencontré sur : feature/hide-bot-staff-docs-c4094a)

- [2026-09-23] `../blueGenjiBot/src/notifications/resolveHandle.ts` (`resolveDiscordHandle`) — la résolution d'un tag balaie **en série** tous les serveurs du bot (`guild.members.fetch({ query })` par la passerelle, un serveur après l'autre) : un tag absent des serveurs partagés les parcourt tous et dépasse les 3 s de `BOT_LOGIN_FETCH_TIMEOUT_MS`, que le site rendait « bot non joignable » (désormais `BOT_RESOLVE_TIMEOUT`). Le site ne dépend plus de cette recherche pour un compte déjà relié à Discord, mais la connexion par code et la certification d'un compte Google y passent encore. Pistes côté bot : interroger d'abord les serveurs BlueGenji (`SERV_GENJI` / `SERV_RIVALS`, cf. `homeGuildIds`), borner la recherche dans le temps et rendre `404` à l'échéance, ou lancer les recherches en parallèle ; et exposer une lecture par identifiant (`client.users.fetch(id)`, sans serveur commun) — hors périmètre (autre dépôt, déploiement du bot) — (rencontré sur : feature/discord-certification-bug-8bd50e)
- [2026-09-23] `docs/DEPLOYMENT.md:89` + `docs/features/USER_AVATAR_IMPORT.md:137` — `npm run backfill:avatars`, « conçu pour tourner en production », y meurt sur `Missing required environment variable DB_HOST` tel que documenté : `lib/server/script-env.ts` choisit ses fichiers d'après `NODE_ENV`, que le shell du serveur n'exporte pas (seul pm2 le pose dans `ecosystem.config.cjs`), si bien que `.env.production` n'est jamais lu. Constaté en production avec `npm run replay:deletions`, qui partage ce chargeur. Piste : documenter `NODE_ENV=production npm run backfill:avatars`, ou faire retomber `loadScriptEnv` sur `production` quand `.env.production` existe et que `NODE_ENV` est absent — (rencontré sur : fix/replay-deletions-node-env)
- [2026-09-23] `lib/server/tournaments/bg-survie.ts:405` (`loadPenalties`) + `:1185` (`loadPenaltyRows`) — les deux lectures de `bg_endurance_penalties` n'ont pas la garde `isMissingTableError` : sur une base où le `CREATE TABLE` avalé de `database.ts` a échoué, **toute réconciliation BG Survie** (donc tout report de score du mode) et la vue du classement lèvent `ER_NO_SUCH_TABLE` — l'inverse exact du contrat « une sanction perdue vaut mieux qu'un report de score en erreur ». Cause : les purges ont été gardées (`ignoreMissingTable`), pas les lectures. Piste : rendre `[]` sur `isMissingTableError` dans les deux fonctions (aucune pénalité n'a pu être posée sans la table). Hors périmètre des purges de `bg_match_reminders` — (rencontré sur : feature/erreur-txt-line-82dfcb, PR #160)

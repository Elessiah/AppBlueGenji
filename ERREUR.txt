# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) — **non corrigé à dessein, prémisse revérifiée le 2026-09-16** : aucune ligne du dépôt ne pose ce `no-store`, c'est le défaut de Next pour une page rendue à la demande, et toutes le sont désormais (la mise en page racine déclare sa dépendance au nonce, voir `lib/shared/csp.ts`). Le rétablir demanderait d'écraser cet en-tête dans le middleware, ce qui rendrait la page restituable par le retour arrière **après une déconnexion** — pseudo et avatar du compte précédent réaffichés sur un poste partagé. La parade existe (`Clear-Site-Data: "cache"` à la déconnexion) mais son support varie selon les navigateurs, et on échangerait une propriété de confidentialité tenue contre un point d'audit. L'entrée reste donc pour qu'on ne le « répare » pas par distraction — c'est sa seule raison d'être — (rencontré sur : feature/pagespeed-audit-fixes ; prémisse revérifiée sur : fix/erreur-txt-cleanup)

- [2026-09-21] `bg_users.email` — colonne d'adresses conservée alors qu'elle n'a plus **aucun** lecteur — le scope `email` a été retiré de la demande faite à Google et le rattachement par adresse supprimé (voir `docs/features/OAUTH_PROVIDERS.md`), si bien que la colonne n'est plus ni lue ni écrite ; les lignes écrites avant gardent pourtant leur adresse. La purger (un `UPDATE … SET email = NULL` idempotent au démarrage, comme la reprise des logos d'entrées solo dans `database.ts`) est la suite logique de la règle « on ne garde que ce qui sert », mais c'est une **destruction irréversible de données de production** que la tâche en cours ne demandait pas : elle doit être décidée explicitement. Retirer aussi la colonne de `exportOwnData` / `PersonalDataExport` le jour où elle est vidée — (rencontré sur : feature/oauth-discord-blizzard)

- [2026-09-21] Les comptes supprimés c'est moche. Si pas de participation à des tournois, supprimer complètement, sinon faire une option décoché par défaut permettant d'afficher les comptes supprimés

- [2026-09-21] Le sous-titre "FREE AGENT" dans chaque carte de joueur dans la page /joueurs ne disparait pas si je décoche "Ouvert aux propositions d'équipes" pourtant lorsque je filtre le profil disparait bien

- [2026-09-21] Les coches pour sélectionner les équipes fantômes à un tournoi ne sont pas stylisé

- [2026-09-21] Faire du tri, dans la page /bot, il n'y a pleins de trucs qui font patterns et qui ne sont pas branchés. Genre la section "Modules", qui n'a pas trop de sens. Les status du tableau "Serveurs connectés" ne sont pas très compréhensible. Pourquoi lag ? (Lien vers la page prod https://bluegenji-esport.fr/bot)
- [2026-09-21] `.github/workflows/ci.yml` (job E2E) + `app/layout.tsx` — le job E2E échoue par intermittence avant d'avoir joué le moindre test : `next dev` n'arrive pas à résoudre `@vercel/turbopack-next/internal/font/google/font` (« next/font/google queries have exactly one entry ») et Playwright abandonne sur `Timed out waiting 120000ms from config.webServer` — cause supposée : `next/font/google` va **chercher les fichiers de police chez Google au démarrage du serveur**, si bien que le job dépend d'un appel sortant vers `fonts.gstatic.com` qu'aucun cache ne couvre ; une relance du seul job passe sans changer une ligne. Piste : héberger les polices (`next/font/local`, fichiers versionnés) — c'est aussi ce que la règle « jamais une origine étrangère » demande déjà pour les images. Hors périmètre de la tâche en cours — (rencontré sur : feature/tournament-blizzard-filters)
- [2026-09-22] `app/(secured)/profil/page.tsx:111,252,282,299` — quatre chemins toastent encore le **code brut** du serveur (`INVITATION_RESPOND_FAILED`, `ACCOUNT_DELETE_FAILED`, `AVATAR_UPLOAD_FAILED`, `AVATAR_DELETE_FAILED`), ce que l'en-tête de `app/(secured)/profil/profile-errors.ts` s'interdit explicitement — cause : ces chemins précèdent le registre, et le lui faire traduire tel quel ne réglerait rien (aucun de ces codes n'y figure, ils tomberaient tous sur « La sauvegarde a échoué », faux pour un téléversement d'avatar comme pour une invitation). Piste : un registre par domaine, ou des entrées nommées dans celui du profil, avec la phrase qui nomme le geste de chacun. Hors périmètre du verrou du tag, qui n'a branché le registre que sur les chemins qu'il touche — (rencontré sur : feature/discord-tag-lock)

- [2026-09-22] `lib/server/users-service.ts:863` (`updateOwnProfile`) — `PATCH /api/profile` avec `{"pseudo": 123}` rend un **400 portant le message interne** `raw.replace is not a function` — cause : le corps du `PATCH` n'est qu'*annoté*, jamais validé, et `patch.pseudo` part tel quel dans `normalizePseudo` (`lib/shared/serialization.ts:38`) ; `app/api/profile/route.ts:46` renvoie ensuite `(error as Error).message` verbatim. Exactement le trou fermé pour `discordPseudo` (contrôle de type dans le service, `INVALID_DISCORD_PSEUDO`) : la parade est la même, un `typeof patch.pseudo !== "string"` → `INVALID_PSEUDO` avant l'appel. Préexistant sur `main`, hors périmètre de la refonte de `/profil` — (rencontré sur : feature/profile-redesign)

- [2026-09-22] `lib/shared/discord-tag-lock.ts:108` (`discordTagLockNotice`) — la phrase du tag non verrouillé dit « Enregistre-le **ci-dessous** » alors que sur `/profil` le `<p id="profile-discord-hint">` qui la rend est le **dernier** enfant du champ : la saisie et le bouton « Enregistrer mon tag » sont au-dessus, et rien ne suit la phrase — cause : texte rédigé sans référence à la position du rendu ; piste : « ci-dessus », ou nommer le bouton plutôt qu'une direction. Même bloc, la phrase enchaîne « personne ne le voit » puis « pour cesser d'être joignable, retire-le », deux affirmations contradictoires pour un tag que la certification n'a pas publié. Ordre identique sur `feature/discord-tag-lock`, donc préexistant à la refonte de `/profil`, qui n'a pas touché ce module — (rencontré sur : feature/profile-redesign)

- [2026-09-22] `lib/server/users-service.ts:980` (`updateOwnProfile`) — sur un compte Discord rattaché, une **correction de casse** du tag (`keryan` → `KERYAN`) répond 200 sans rien écrire : le refus lisible la laisse passer (comparaison `sensitivity: "base"`, volontairement, « sans quoi une correction de casse serait refusée ») mais la branche `WHEN discord_id IS NOT NULL AND ? IS NOT NULL THEN discord_pseudo` du `CASE` — le garde-course qui suit — jette **toute** écriture non nulle d'un compte rattaché. Les deux gardes ne s'accordent donc pas sur ce qui est permis, et `getFullProfile` renvoie l'ancienne valeur sans un mot. Piste : comparer dans le `CASE` contre la valeur stockée (`discord_pseudo <=> ?` comme le fait la branche voisine de `discord_verified_at`) plutôt que de garder en bloc. Venu de `feature/discord-tag-lock`, module non touché par la refonte de `/profil` — (rencontré sur : feature/profile-redesign)

- [2026-09-22] `app/(secured)/profil/page.tsx` (`loadDiscordState`) — un échec de `GET /api/profile/discord` (réponse non-ok ou coupure) laisse `linked` à `null`, valeur **indiscernable** de « pas encore lu » : un compte sans aucun rattachement Discord se voit alors verrouiller son champ et annoncer « Impossible de lire l'état de ton compte Discord pour l'instant », et la sortie proposée (« Recharge la page ») rejoue la même requête. Le silence est assumé dans le module (`lib/shared/discord-tag-lock.ts`, commentaires de `loadDiscordState`), mais un troisième état (`loaded` / `error`) rendrait le cas transitoire comme un chargement. Fonction inchangée depuis `feature/discord-tag-lock` — (rencontré sur : feature/profile-redesign)

- [2026-09-22] `lib/shared/discord-tag-lock.ts:116` (`discordTagLockNotice`, branche certifiée) — la phrase « les administrateurs le voient, et les arbitres pendant tes tournois » **recopie** la promesse que `lib/shared/identity-sharing.ts` tient en un seul endroit (`DISCORD_TAG_AUDIENCE`), et c'est la seule que `/profil` affiche au tag certifié — un ajustement de l'une laisserait l'autre promettre autre chose, exactement l'écart que le module partagé existe pour empêcher. Piste : composer la phrase du verrou depuis `DISCORD_TAG_AUDIENCE`. Module venu de `feature/discord-tag-lock`, non touché par la refonte de `/profil` — (rencontré sur : feature/profile-redesign)

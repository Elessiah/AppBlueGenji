# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.
- [2026-09-07] lib/server/tournaments/bg-survie.ts (forfeitEnduranceTeam) — un abandon déclaré sur un tournoi BG Survie **déjà terminé** écrit bien `status = 'FORFEIT'` et clôt le match en cours, mais `reconcileEndurance` sort aussitôt sur sa garde `state === "FINISHED"` : le classement n'est jamais rejoué et `final_rank` garde l'ancien podium, si bien que la page (qui rejoue toujours, via `loadEnduranceMeta`) contredit le palmarès stocké ; `forfeitSurvivalTeam` (survival.ts:616) et `forfeitSwissTeam` (swiss.ts:653) posent tous deux `state !== "RUNNING"` → `TOURNAMENT_NOT_RUNNING`, la garde manque au seul mode endurance — le cas est atteignable, un tournoi clos par `startEndurancePlayoffs` faute de qualifiées garde `endurance_playoffs_started` à 0 ; piste : ajouter la même garde (c'est ce que font désormais `applyEndurancePenalty` / `liftEndurancePenalty`), ou lever la coupure `FINISHED` des trois réconciliations, comme le propose déjà l'entrée précédente — (rencontré sur : feature/endurance-penalties, PR #98)
- [2026-09-08] lib/server/tournaments/phases.ts:211 (`reconcilePhases`, garde `state !== "RUNNING"`) — corriger le score de la finale d'un tournoi **MULTI** déjà terminé change bien `winner_team_id` en base, mais ne recalcule ni le podium ni `final_rank` : le tournoi garde l'ancienne championne à son palmarès ; c'est le même défaut que celui des trois modes à classement (réglé par `docs/features/FINISHED_TOURNAMENT_RECONCILIATION.md`), mais il ne se règle pas en levant une seule garde — `reconcilePhases` sort sur l'état du tournoi *et* sur celui de la phase courante (`currentPhase.state !== "RUNNING"`), or la phase finale d'un tournoi clos est close elle aussi ; piste : rejouer la finalisation de la dernière phase (classement de phase puis `finalizeMultiTournament`) sans jamais en démarrer une nouvelle, en gardant la règle « le classement se rejoue, le tournoi ne se rouvre pas » — (rencontré sur : feature/finished-reconciliation)

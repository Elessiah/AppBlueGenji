# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) — **non corrigé à dessein, prémisse revérifiée le 2026-09-16** : aucune ligne du dépôt ne pose ce `no-store`, c'est le défaut de Next pour une page rendue à la demande, et toutes le sont désormais (la mise en page racine déclare sa dépendance au nonce, voir `lib/shared/csp.ts`). Le rétablir demanderait d'écraser cet en-tête dans le middleware, ce qui rendrait la page restituable par le retour arrière **après une déconnexion** — pseudo et avatar du compte précédent réaffichés sur un poste partagé. La parade existe (`Clear-Site-Data: "cache"` à la déconnexion) mais son support varie selon les navigateurs, et on échangerait une propriété de confidentialité tenue contre un point d'audit. L'entrée reste donc pour qu'on ne le « répare » pas par distraction — c'est sa seule raison d'être — (rencontré sur : feature/pagespeed-audit-fixes ; prémisse revérifiée sur : fix/erreur-txt-cleanup)

- [2026-09-23] `../blueGenjiBot/src/notifications/resolveHandle.ts` (`resolveDiscordHandle`) — la résolution d'un tag balaie **en série** tous les serveurs du bot (`guild.members.fetch({ query })` par la passerelle, un serveur après l'autre) : un tag absent des serveurs partagés les parcourt tous et dépasse les 3 s de `BOT_LOGIN_FETCH_TIMEOUT_MS`, que le site rendait « bot non joignable » (désormais `BOT_RESOLVE_TIMEOUT`). Le site ne dépend plus de cette recherche pour un compte déjà relié à Discord, mais la connexion par code et la certification d'un compte Google y passent encore. Pistes côté bot : interroger d'abord les serveurs BlueGenji (`SERV_GENJI` / `SERV_RIVALS`, cf. `homeGuildIds`), borner la recherche dans le temps et rendre `404` à l'échéance, ou lancer les recherches en parallèle ; et exposer une lecture par identifiant (`client.users.fetch(id)`, sans serveur commun) — hors périmètre (autre dépôt, déploiement du bot) — (rencontré sur : feature/discord-certification-bug-8bd50e)

- [2026-09-23] `tests/lib/server/teams-service.test.ts` (28 cas), `tests/app/api/profile/route.test.ts` (7 cas), une bonne part de `tests/lib/server/tournaments-service.matches.test.ts` — **tests tautologiques** : ils comparent des constantes locales (`const requesterRole = "DPS"; expect(requesterRole === "OWNER").toBe(false)`) sans importer ni exercer le code qu'ils prétendent tester (le premier fichier n'importe **rien** de `lib/`). La type-vérification des tests les a révélés (TS2367, « comparaison sans recouvrement ») ; ils ont seulement été annotés (`const x: string = …`) pour compiler, leur sens n'ayant pas été touché. Piste : les réécrire contre `userCanManageTeam` / `hasTeamManagementRole`, `reportMatchScore` et les routes de `/api/profile`, ou les supprimer — ils gonflent le compteur sans rien garder. Hors périmètre — (rencontré sur : feature/ci-reliability)

- [2026-09-23] `tests/helpers/fake-db.ts` — helper **mort** : aucune suite ne l'importe (vérifié par `grep`). Il a été rendu compilable (type de retour de `executeSql`) pour que `npm run typecheck` passe, rien de plus. Piste : le supprimer. Hors périmètre — (rencontré sur : feature/ci-reliability)

- [2026-09-23] `playwright.config.ts` (`webServer`) — `npm run test:e2e` lancé en local échoue sur `e2e/auth.spec.ts` « Consentement RGPD › le refus ramène à l'accueil » dès que le `.env` du poste pose `DEV_AUTH_USER_ID` : le commentaire de `reuseExistingServer: false` promet un serveur « sans bypass d'auth », mais le serveur neuf relit le même `.env` et démarre **connecté**. Le CI, sans `.env`, passe (12/12, vérifié aussi en local en retirant le fichier). Piste : neutraliser la variable dans `webServer.env` — à vérifier que `@next/env` ne la réécrase pas depuis le `.env` (une valeur vide peut compter pour absente) ; sinon, une valeur non entière suffit, le bypass exigeant un identifiant valide. Hors périmètre — (rencontré sur : feature/ci-reliability)

- [2026-09-23] `tests/**` (~460 `mockResolvedValue(x as never)` et consorts) — les valeurs simulées ne sont **pas** type-vérifiées : un `jest.fn()` ou un `as jest.Mock` sans type a `Mock<UnknownFunction>` pour type, dont le paramètre de `mockResolvedValue` vaut `never`, d'où le `as never` — convention antérieure, étendue par la PR qui a branché `npm run typecheck` pour éponger ~460 erreurs d'un coup. Un champ ajouté à `AuthUser` ou au retour d'un service ne se voit donc toujours pas dans ces valeurs. Piste : migrer fichier par fichier vers `jest.mocked(fn).mockResolvedValue(…)` (ou `jest.fn<typeof fn>()`), qui vérifient la valeur contre le vrai type de retour ; la règle est déjà écrite dans CLAUDE.md pour les tests neufs. Hors périmètre — (rencontré sur : feature/ci-reliability)

- [2026-09-23] `lib/server/seed.ts:979` (`generateEnduranceTournament`) → `lib/server/tournaments/bg-survie.ts:180` (`persistStandings`) — `npm run seed` s'arrête sur `ER_NO_REFERENCED_ROW_2` : l'insertion dans `bg_endurance_standings` ne trouve pas le tournoi (`fk_endurance_standings_tournament`), alors que le seed vient de le créer. Rencontré sur une base déjà seedée (le nettoyage a bien tourné). Cause supposée : le tournoi est inséré sur une connexion ou une transaction que `reconcileEndurance` ne voit pas encore (écriture non validée lue depuis le pool). Aucun fichier du seed ni du moteur n'est touché par la branche où il a été vu — (rencontré sur : feature/ci-reliability)

# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-01] app/(secured)/joueurs/[id]/page.tsx:104, app/(secured)/profil/page.tsx:209, components/arena-nav.tsx:72 — avatar de repli cassé (404) pour tout compte sans avatar — `/vercel.svg` est référencé comme `src` de repli mais le fichier n'existe pas dans `public/` (reste du template Next.js, supprimé depuis) ; remplacer par un repli existant ou par la pastille à initiale déjà utilisée dans les cartes d'annuaire — (rencontré sur : feature/clickable-teams-players-9ae592)
- [2026-09-01] app/globals.css:1 — tous les tokens hérités (`--text-0/1/2`, `--bg-0/1/2`, `--accent-*`, `--line`, `--radius`, `--danger`…) sont indéfinis sur tout le site : `body { color: var(--text-0) }` retombe sur du noir, d'où des textes noirs sur fond noir partout où la couleur est héritée — le fichier commence par un BOM UTF-8 (`efbbbf`) qui rend invalide le premier bloc `:root`, silencieusement ignoré par le parseur CSS ; le second `:root` (tokens cyber `--ink`, `--blue-*`) est intact, ce qui masque le problème sur la plupart des écrans. Retirer le BOM (`git show HEAD:app/globals.css | tail -c +4`) et vérifier que `getComputedStyle(document.documentElement).getPropertyValue('--text-0')` n'est plus vide — (rencontré sur : feature/clickable-teams-players-9ae592)
- [2026-09-01] app/(secured)/tournois/[id]/_components/SwissView.tsx:200 et SurvivalView.tsx:184 — dans le classement d'une ronde suisse / d'une survie, le nom de l'engagé est écrasé à ~4 px de large et devient illisible dès que le bouton « Abandonner » est rendu (vue staff, tournoi en cours) — la colonne du classement est figée à `flex: 0 0 400px` et les 7 autres enfants de la ligne plus les `gap: 10px` consomment ~394 px, le nom étant le seul en `flex: 1` avec `overflow: hidden` ; donner une largeur minimale au nom, ou replier statut/action sur une seconde ligne — (rencontré sur : feature/clickable-teams-players-9ae592)

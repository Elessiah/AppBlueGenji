# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) — **non corrigé à dessein, prémisse revérifiée le 2026-09-16** : aucune ligne du dépôt ne pose ce `no-store`, c'est le défaut de Next pour une page rendue à la demande, et toutes le sont désormais (la mise en page racine déclare sa dépendance au nonce, voir `lib/shared/csp.ts`). Le rétablir demanderait d'écraser cet en-tête dans le middleware, ce qui rendrait la page restituable par le retour arrière **après une déconnexion** — pseudo et avatar du compte précédent réaffichés sur un poste partagé. La parade existe (`Clear-Site-Data: "cache"` à la déconnexion) mais son support varie selon les navigateurs, et on échangerait une propriété de confidentialité tenue contre un point d'audit. L'entrée reste donc pour qu'on ne le « répare » pas par distraction — c'est sa seule raison d'être — (rencontré sur : feature/pagespeed-audit-fixes ; prémisse revérifiée sur : fix/erreur-txt-cleanup)
- [2026-09-24] `lib/server/tournaments/validation.ts` (`validateRawPhases`, boucle `INVALID_QUALIFIER_COUNT`) — un plan multi-phases valide est refusé en 400 à la création : la comparaison des cibles de qualification inclut la **dernière** phase, dont la cible n'est jamais lue et que `PhaseCard` masque (`!isLast`). Exemple atteignable depuis le formulaire : ronde suisse à 50 % puis finale en Survie, créée avec `PERCENT 100` par `createDefaultPhase` — `validatePhases` côté client passe (il s'arrête à `i + 2 < length`), le serveur refuse, et la notification demande de corriger une qualification que l'écran ne montre pas. Piste : borner la boucle comme `validatePhases` (`i + 2 < list.length`), et trancher si la comparaison des **pourcentages** doit subsister — 80 % après 50 % qualifie moins d'engagés, et `validatePhases` ne la fait pas (le test « rejette un plan avec PERCENT qui monte » de `create-multi.test.ts` la fige) — (rencontré sur : feature/tournament-form-error-messages)
- [2026-09-25] `components/cyber/landing/Hero.module.css:18` (`.left` / `.right`) + `components/cyber/CountdownStrip.tsx` — à 375 px, tout le hero déborde de ~13 px à droite et le `overflow: hidden` de `.root` rogne la fin du texte d'intro (« Overwatch… »), le bouton « Regarder le live » et la 4ᵉ case du compte à rebours : le compte à rebours (étiquette 70 px + cases 246 px) impose 376 px de largeur minimale à la piste `1fr` de la grille, pour 351 px disponibles — piste : `min-width: 0` sur `.left` et `.right`, et un `CountdownStrip` qui passe l'étiquette au-dessus des cases sous ~420 px — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/LiveCard.tsx:46` (`nextDaysLabel`) — carte sans tournoi en cours : « Le prochain démarre dans **bientôt** » quand aucun tournoi n'est prévu, « dans **aujourd'hui** » le jour même — la fonction rend un complément qui ne se construit pas avec « dans » — piste : rendre la phrase entière selon le cas (« Aucun tournoi programmé », « Le prochain démarre aujourd'hui », « … dans N jours ») — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/CalendarCard.tsx` — aucun état vide : sans événement, la carte « Prochains événements » n'affiche qu'un en-tête — piste : une phrase « Aucun tournoi programmé » — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/Hero.tsx:96` — « Tournois organisés » affiche `19 + stats.tournaments` : décalage magique non documenté, invérifiable et à maintenir à la main — piste : constante nommée et commentée (tournois antérieurs au site), ou valeur éditable dans `bg_settings` — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/LiveCard.tsx:123-126,172` — la carte du direct montre des données sans sens pour un visiteur : « MATCH 263559 » (identifiant de base), « 👁 0 » (audience nulle affichée), « ROUND 2 » (anglais, via `inferPhaseLabel`) — piste : retirer l'identifiant (ou le remplacer par le libellé de manche), masquer l'audience sous un seuil, franciser « Manche » — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/CalendarCard.tsx:17` (`tagLabel`) + `getLandingCalendar` — « Prochains événements » liste des tournois déjà **en cours** et datés dans le passé, et un tournoi `UPCOMING` (inscriptions pas encore ouvertes) est étiqueté « OPEN » (faux, et en anglais) — piste : titre « En cours et à venir » ou filtre, libellés « Inscriptions ouvertes » / « Bientôt » — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `lib/shared/about-pillars.ts:21-31` (valeurs par défaut des piliers) — jargon anglais (« rulebook versionné », « watch parties », « scoreboard ») et promesse que le site ne tient pas (« matchmaking par niveau ») — piste : réécrire en français, retirer ce que le site ne fait pas (les valeurs en base s'éditent depuis l'accueil) — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/Hero.tsx` / `Hero.module.css` — le bouton principal « Inscrire mon équipe » tombe à 862–903 px sur un écran de 900 px (vue admin), donc sous la ligne de flottaison dès 1366×768 : le titre en 82 px sur quatre lignes occupe le premier écran ; trois appels à l'action concurrents (inscription, live, bloc Discord plein, plus saillant que le bouton principal) — piste : titre plus compact, bloc Discord en retrait visuel, bouton principal remonté — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] accueil `/` en mobile — page de 8 862 px (~11 écrans à 375×812), chiffres du hero repliés sur deux lignes avec séparateurs orphelins (`.sep`), étiquette du compte à rebours écrasée sur quatre lignes — piste : grille à trois colonnes pour les chiffres, sections condensées en mobile (partenaires, piliers) — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `TournamentBoard.tsx:28`, `LeaderCal.tsx:16`, `AboutSection.tsx:21`, `SponsorsGrid.tsx:230` (`components/cyber/landing/`) — sur-titres « SECTION 01…04 » et méta-lignes décoratives (« EN COURS · INSCRIPTIONS · BRACKETS », « SAISON EN COURS ») sans information — piste : les retirer, ou les remplacer par un fait utile (nombre de tournois ouverts…) — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/CalendarCard.tsx:30` — lien « ICS → » cryptique pour un visiteur — piste : « Ajouter à mon agenda (.ics) » — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/landing/PublicFooter.tsx:81-84` — deux liens voisins presque homonymes, « Accessibilité » (ouvre le menu) et « Accessibilité : non conforme » (mention RGAA obligatoire, à garder telle quelle) — piste : renommer le premier « Réglages d'accessibilité » — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/CountdownStrip.tsx:17` — avant hydratation, le compte à rebours s'affiche « 00 00 00 00 » (horloge `null` au rendu serveur), comme si le tournoi démarrait — piste : placeholder « -- », ou valeur calculée côté serveur — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `components/cyber/CountdownStrip.tsx` — compte à rebours sans `<time datetime>` ni texte accessible : un lecteur d'écran lit « 01 J 23 H 21 M 31 S » — piste : `<time>` + libellé `sr-only` (« Début dans 1 jour 23 heures ») et cases `aria-hidden` — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] bouton flottant d'accessibilité (bas gauche), accueil en mobile — recouvre du contenu (numéro « 03 » des piliers, texte de la carte « Communautaire ») faute de marge réservée en bas de page — piste : `padding-bottom` sur la page à hauteur du bouton — (rencontré sur : feature/audit-homepage-ui-ux-7fbd1a, audit UI/UX de l'accueil)
- [2026-09-25] `app/(secured)/tournois/page.tsx` (bandeau de chiffres) — « 46 EN DIRECT · Diffusés sur Twitch » compte les tournois `RUNNING`, pas les diffusions : aucune chaîne n'est vérifiée, et Twitch est nommé en dur (YouTube et Kick sont aussi acceptés). Le quatrième chiffre affiche « — / Prizepool · à venir » à tout non-staff : un emplacement réservé laissé en production — piste : « N en cours », un compteur de diffusions seulement s'il s'appuie sur `isMatchLive` ; retirer la case prizepool tant qu'elle n'existe pas — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/_lib/ticker.ts` — chaque tournoi en cours donne « RÉSULTAT · <nom> · N équipes engagées » alors qu'aucun résultat n'est porté ; et les en-cours ne sont pas bornés (46 entrées dans le seed) quand les inscriptions sont limitées à 3 et les à-venir à 2 : le bandeau ne parle plus que d'eux — piste : « EN COURS · … », et une borne comme pour les deux autres paniers — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/page.tsx` + `_lib/buckets.ts` (recherche et filtres) — le placeholder promet « un tournoi, une équipe, un format… » mais `filterTournamentsByQuery` ne lit que nom et description ; le champ n'a ni `<label>` ni `aria-label` (placeholder seul) ; le raccourci s'affiche « ⌘K » sur Windows (Ctrl+K fonctionne) ; les pastilles de jeu n'ont pas `aria-pressed` et leur nom accessible colle le chiffre (« Tous83 ») ; leurs compteurs ignorent la recherche alors que les sections la suivent ; et une section vidée par un filtre dit « Aucun tournoi en cours actuellement » au lieu de « aucun résultat pour cette recherche » — piste : aligner le placeholder sur ce qui est filtré (ou filtrer aussi format et engagés), `aria-label`, `aria-pressed`, libellé du raccourci selon la plateforme, message vide conditionné au filtre — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/Section.tsx` + `page.tsx` (balisage) — l'en-tête de section est un `<h2>` **dans** un `<button>` (contenu non autorisé : un bouton n'accepte que du contenu phrasé), sans `aria-controls` ; « Créer un tournoi » est un `<button>` dans un `<Link>` (contrôle interactif imbriqué, deux arrêts de tabulation) ; chaque carte est un lien unique dont le nom accessible concatène tout son texte (170 à 240 caractères mesurés) ; le bloc vide titre « Vide » — piste : `<h2><button aria-expanded aria-controls>…</button></h2>`, `CyberButton asChild`, plaque `.cardOverlay` au nom court (le titre) comme sur les annuaires — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/page.tsx` (volume) — rien ne borne les sections vivantes : avec les 46 tournois en cours du seed, « EN COURS » occupe ~9 200 px à 1024 px de large (cartes de 373 px, deux par ligne) et ~20 000 px sur mobile (453 px, une par ligne) avant d'atteindre les inscriptions ouvertes ; aucun tri ni vue compacte. Côté « Terminés », « Voir tout » est irréversible (pas de « Voir moins ») et stylé en ligne — piste : même limite + « Voir plus » sur toutes les sections, ou une vue liste compacte — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/_hooks/useTournamentLive.ts` (`connect`) — si le flux SSE s'**ouvre** (200, `onopen`) mais que le premier instantané n'arrive jamais (proxy ou antivirus qui met la réponse en tampon), la fiche reste sur « Chargement du tournoi… » **indéfiniment** : le repli REST n'est déclenché que par `onerror`, et `onopen` coupe même le sondage de secours. Reproduit dans le navigateur intégré de l'app desktop (flux ouvert, aucune donnée, alors que `GET /api/tournaments/14431` répond 200 en 1,6 s). L'état de chargement est en outre un texte nu, sans squelette — piste : minuteur « premier instantané » (quelques secondes) qui appelle `load()` et démarre le sondage si rien n'est arrivé — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/_components/MatchRow.tsx` (cycle de report) — l'état `AWAITING_CONFIRMATION` et `scoreDeadlineAt` ne sont lus par **aucun** composant de `app/` ni de `components/` : après un report, la carte ne dit rien, l'équipe qui a saisi retrouve le même formulaire vide (elle ne sait pas si c'est parti), l'adversaire doit retaper le score à l'aveugle — un chiffre différent et c'est un conflit —, et personne ne voit le délai avant validation d'office. `BracketMatch` ne transporte d'ailleurs pas le score reporté — piste : bandeau « Score reporté par X : 2-1 · à confirmer avant HH:MM » avec « Confirmer / Contester », ce qui demande d'ajouter le report en attente à l'instantané — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/_components/MatchRow.tsx` (formulaire de score) — les champs s'appellent « Moi / Eux » quelle que soit la place de l'équipe du lecteur sur la carte (équipe 1 ou 2), si bien que l'ordre des champs peut être l'inverse de celui des noms juste au-dessus ; le bouton d'envoi « ✓ » n'a pas de nom accessible ; et la notification dit « Score transmis pour le match #4521 » (identifiant interne, `page.tsx:376`) — piste : nommer les champs par les équipes, dans l'ordre de la carte, bouton « Envoyer le score », message « Score transmis : A 2 – 1 B » — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/page.tsx` (`IssueReportProvider canReport`) + `MatchRow.tsx` — dès que le lecteur est engagé, **chaque** carte du plateau porte « ⚠ Signaler un problème », y compris les matchs des autres équipes : 127 boutons identiques sur un plateau à 128, qui allongent chaque carte et noient le formulaire de score. Conforme à la règle écrite (« sur chaque match, réservé aux engagés ») : à trancher plutôt qu'à corriger — piste : ne l'afficher que sur les matchs du lecteur, le bouton d'en-tête couvrant le reste — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/page.tsx` + `_components/TournamentHeader.tsx` (parcours) — « ← Retour » appelle `router.back()` : arrivé par un lien partagé ou le lien profond de l'accueil (`#match-…`), on quitte le site ou on revient sur un onglet vide ; la frise de progression (`TournamentProgress`) est rendue tout en bas, sous inscrites et contacts, loin de l'en-tête qu'elle complète ; le forfait, le retrait de pénalité et le lancement forcé (`MatchLaunchStrip`) passent par `window.confirm` quand les autres gestes irréversibles ont une modale — piste : lien vers `/tournois` (ou `back()` seulement si l'historique vient du site), frise sous l'en-tête, modale commune pour les confirmations — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `app/(secured)/tournois/[id]/page.tsx` + `_components/MatchRow.tsx` (dette de style) — styles en ligne et classes/jetons d'avant la refonte (`ds-block`, `ds-section-title green`, `ds-title green`, `btn`, `var(--text-2)`, `var(--border, #444)`, couleurs `rgba(...)` littérales dont l'orange du forfait, que « Contraste renforcé » ne peut pas atteindre) ; le bloc « Qualifiées / Classement » d'une phase terminée est recopié trois fois avec un `<div>` pour titre, et s'intitule « Classement » dans une branche et « Qualifiées » dans les deux autres pour le même contenu — piste : extraire un composant avec un vrai titre, passer MatchRow en CSS Module sur les jetons cyber — (rencontré sur : feature/audit-tournois-ui-ux-c9d7d5)
- [2026-09-25] `lib/server/tournaments/snapshot.ts` (`loadMaintainedRow`) — une manche tranchée par l'expiration du délai **pendant la reconstruction d'un instantané** (et non par la passe de fond) n'invalide que les listes : le classement du site (`ranking:`, rejeu Elo) et la vitrine (`landing:`) gardent l'ancien résultat jusqu'à 60 s, là où la même manche tranchée par `syncVisibleTournaments` passe par `publishUpdatedEvent` et les vide. Sans gravité (le TTL borne la dérive), mais les deux chemins d'entretien ne publient plus à la même mesure. Piste : sur `contentChanged`, appeler aussi `invalidateTeamRanking()` et `invalidateLandingAggregates()` — ou faire rendre à `syncTournamentState` un `resultsChanged` distinct de la création de plateau — (rencontré sur : feature/tournament-server-network-optimization-7fffc3)
- [2026-09-25] `components/cyber/landing/TournamentBoard.tsx` (`boardActionLabel`) — le tableau des tournois de l'accueil propose « S'inscrire » aux inscriptions ouvertes avec une place libre, sans connaître le lecteur : un visiteur sans équipe, sans rôle de gestion ou déjà inscrit clique et arrive sur une fiche qui refuse l'inscription (`registerBlockedNotice`) — même défaut que les cartes d'inscription de `/tournois` — piste : « Voir le tournoi » par défaut, « S'inscrire » seulement avec le contexte du lecteur — (rencontré sur : feature/landing-tournament-board)

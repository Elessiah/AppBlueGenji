# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.

- [2026-09-09] lib/shared/landing.ts:chooseFeaturedTournament + composants de l'accueil — le hero annonce « PROCHAIN TOURNOI · <nom> » et décompte jusqu'au tournoi à venir le plus LOINTAIN, pas le plus proche ; la grille de la section montre de même les trois tournois qui démarrent le plus tard — `listTournamentBuckets` trie `ORDER BY t.start_at DESC` (correct pour la liste des terminés de /tournois), et l'accueil lit `buckets.upcoming[0]` puis `.slice(0, 3)` sans retrier ; piste : trier par `startAt` croissant dans le module pur `lib/shared/landing.ts` (ne pas toucher à l'ORDER BY, partagé avec /tournois) — (rencontré sur : feature/finished-tournament-active-list-4f09e8 / PR #112)

- [2026-09-09] components/cyber/landing/LiveCard.tsx — les noms d'équipe de la carte « en direct » sont des cibles de 18 px de haut, sous le minimum de 24 px (Lighthouse `target-size`, poids 7, accessibilité de l'accueil à 96 au lieu de 100) ; invisible sur la production au moment de l'audit, la carte n'apparaissant qu'avec un match en cours — piste : `display: inline-block` + `padding` vertical sur `.nested`, à vérifier à l'œil, la carte étant serrée — (rencontré sur : feature/pagespeed-audit-fixes)
- [2026-09-09] components/recruitment-highlight.tsx — sur mobile, la modale de recrutement est l'élément LCP de l'accueil avec 3,8 s de « render delay » (LCP 4,4 s, performance 84) : elle n'est peinte qu'après l'hydratation puis un aller-retour vers `/api/recruitment/highlight` ; sans elle, le LCP retomberait au FCP (1,6 s) — piste : passer l'annonce en prop depuis le rendu serveur (supprime l'aller-retour) et n'en décider l'affichage qu'ensuite ; rendre la modale dans le HTML initial demanderait un script en ligne pour éviter le clignotement chez qui l'a déjà écartée — (rencontré sur : feature/pagespeed-audit-fixes)
- [2026-09-09] accueil `/` — `bf-cache` échoue (`Cache-Control: private, no-cache, no-store` sur le document) : **non corrigé à dessein**, la page porte l'en-tête de session (pseudo et avatar du connecté) et un retour arrière restituant la page d'un autre compte serait pire que le gain ; noté pour qu'on ne le « répare » pas par distraction — (rencontré sur : feature/pagespeed-audit-fixes)
- [2026-09-09] lib/server/database.ts:244-257 — `npm run seed` échoue sur « Data truncated for column 'game' » à la création des tournois, et toute création de tournoi OW2 échouerait de même : la migration ne fait qu'`ADD COLUMN game ENUM('OW2','MR')`, si bien qu'une base créée du temps où la valeur s'écrivait `'OW'` garde `enum('OW','MR')` à jamais (constaté en local : `SHOW COLUMNS FROM bg_tournaments LIKE 'game'` → `enum('OW','MR')`) — piste : migration en trois temps, élargir en `ENUM('OW','MR','OW2')`, `UPDATE … SET game='OW2' WHERE game='OW'`, puis resserrer en `ENUM('OW2','MR')` ; sans elle le jeu de test est irrécupérable sur toute base antérieure au renommage — (rencontré sur : feature/pagespeed-audit-fixes)

- [2026-09-09] lib/server/api-guard.ts:requestClientIp — le repli sur l'en-tête `x-real-ip` quand `X-Forwarded-For` est absent **affaiblit** les plafonds par IP au lieu de les servir : il ne se déclenche que là où aucun relais de confiance n'a écrit `X-Forwarded-For`, donc précisément là où l'en-tête est forgeable par le client, qui obtient alors un seau neuf à chaque requête (vitrine, visites, relais de logo, flux du bot) ; dans un déploiement correctement mandaté il est mort, `X-Forwarded-For` étant toujours présent — **non corrigé à dessein** : le retirer désarmerait un déploiement qui ne poserait que `X-Real-IP`, choix qui appartient à l'exploitant, et le module documente déjà « on préfère ne pas compter que compter faux » ; côté code de connexion Discord, la demande porte sur le seul identifiant visé et n'en dépend donc pas, mais la **vérification** est depuis passée sur le couple (compte visé, IP appelante) pour cesser de punir la victime : un appelant qui forge l'en-tête obtient là un seau neuf à chaque essai — sans gain pour lui, le quota d'essais qui tient réellement le secret vivant en base, sur la ligne du code — (rencontré sur : feature/authorization-audit-fixes / PR #116)

- [2026-09-15] package.json (dépendance `next`) — `npm audit` signale toujours 2 vulnérabilités (1 modérée, 1 haute) après la PR #119, via `postcss` (XSS sur la sortie CSS stringifiée, lecture de fichiers arbitraires par `sourceMappingURL`) tiré transitivement par `next` — confirmé préexistant sur `main` (même sortie d'audit avant la PR), donc hors périmètre d'une PR titrée « 2 critiques » (ces deux-là ne sont pas critiques) ; `npm audit fix --force` proposerait une résolution avec changements cassants sur `next`, à évaluer séparément — (rencontré sur : fix/dependency-security-updates / PR #119)
- [2026-09-16] déploiement (nginx devant `next start`) — `www.bluegenji-esport.fr` est un piège armé : le HSTS annonce `includeSubDomains`, donc tout navigateur ayant vu l'apex en HTTPS forcera aussi le HTTPS sur `www`, or il n'existe aucun bloc `server` en 443 pour `www`, aucun `default_server` en 443, et le certificat ne porte que `DNS:bluegenji-esport.fr` — l'erreur de certificat serait alors incontournable, le HSTS retirant le « continuer quand même », et la redirection `www` du port 80 ne s'exécuterait jamais (le navigateur n'émet plus de requête en clair) ; elle renvoie d'ailleurs vers `http://` et non `https://`. Sans effet aujourd'hui : `www` ne résout pas. Piste : étendre le certificat à `www` et poser un bloc 443 qui redirige vers l'apex, **avant** d'ajouter l'enregistrement DNS — la configuration nginx n'est pas dans ce dépôt — (rencontré sur : feature/proxy-google-avatars)

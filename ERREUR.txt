# ERREUR.txt — erreurs rencontrées mais non réglées (préexistantes / hors périmètre)
#
# Format d'une entrée :
#   - [AAAA-MM-JJ] <fichier:ligne ou zone> — <symptôme observé> — <cause supposée / piste> — (rencontré sur : <branche ou PR>)
#
# Ajouter à la fin. Vérifier l'absence de doublon avant d'ajouter.
# Retirer l'entrée dans le commit qui règle l'erreur.
- [2026-09-07] lib/server/database.ts:245 (runMigrations, ALTER TABLE bg_tournaments ADD COLUMN game) — après ~20 min de `next dev`, toute page protégée tombe en « Application error » avec ER_LOCK_DEADLOCK ; l'échec est définitif (la promesse mémorisée par `ensureMigrations` reste rejetée, chaque requête suivante rejoue le même échec) et seul un redémarrage du serveur débloque — les migrations sont rejouées à chaque démarrage, sans verrou d'exclusion inter-processus ni reprise après un ER_LOCK_DEADLOCK, or MySQL déclare un interblocage entre deux ALTER TABLE concurrents sur la même table ; pistes : mémoriser la promesse seulement en cas de succès, et prendre un GET_LOCK() nommé autour de `runMigrations` — (rencontré sur : feature/seeding-drag-and-drop, PR #95)
- [2026-09-07] lib/server/tournaments/bg-survie.ts:354 / survival.ts:536 / swiss.ts:580 (garde `state === "FINISHED"` des trois réconciliations) — corriger le score de la **finale** d'un tournoi déjà terminé change bien `winner_team_id` en base, mais ne recalcule ni le podium ni `final_rank` : le tournoi garde l'ancienne championne à son palmarès ; `adminResolveMatch` n'a aucune garde d'état (par choix : corriger une archive doit rester possible, et `finishTournament` prévoit explicitement le rejeu de la finalisation), mais les trois réconciliations sortent immédiatement sur un tournoi `FINISHED`, si bien que `finalizeEndurance` / son équivalent n'est jamais rappelé — piste : laisser la réconciliation s'exécuter sur un tournoi terminé (elle est idempotente, et `finishTournament` ne réécrit ni `finished_at` ni la ligne de journal grâce à sa clause `state <> 'FINISHED'`), plutôt que de la couper en tête ; concerne les trois modes à classement de la même façon, donc à traiter d'un seul tenant — (rencontré sur : feature/bg-survie-playoff-repair)

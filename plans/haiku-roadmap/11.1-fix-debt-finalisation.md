# 11.1 — Finir les tâches 10 et 11 (refactor incomplet + lint cassé)

Les tâches 10 et 11 ont été lancées en parallèle et n'ont pas été menées à terme. Cette tâche corrige les résidus.

## Problèmes constatés

### Résidu tâche 10 — split `tournaments-service`

1. **Build cassé** : ESLint refuse `any[]` dans [lib/server/tournaments/index.ts](../../lib/server/tournaments/index.ts) ligne 209.
2. **Le `index.ts` du nouveau module n'est pas un simple point d'entrée** : il fait 487 lignes et contient encore du code métier (`syncVisibleTournaments`, `listTournamentBuckets`, `createTournament`, `getTournamentDetail`). Il devait être un index de ré-exports.

### Résidu tâche 11 — allègement page tournoi détail

3. **Page toujours à 456 lignes** : [app/(secured)/tournois/[id]/page.tsx](../../app/(secured)/tournois/[id]/page.tsx) garde inline le composant `BracketTree` (lignes 28–220).
4. **Hook `useScoreForm` non utilisé** : [_hooks/useScoreForm.ts](../../app/(secured)/tournois/[id]/_hooks/useScoreForm.ts) existe mais aucun import dans `page.tsx`. La logique `submitScore` est restée inline ligne 263.
5. **Hook `useKeyboardShortcuts` non utilisé** : créé puis oublié.
6. **`ScoreInputDialog` mal câblé** : aujourd'hui ouvert via `selectedMatchForAdmin` alors que ce dialog devait servir à la saisie de score utilisateur. Vérifier l'intent, distinguer l'admin du joueur.

## Objectif

Terminer proprement les deux tâches sans casser le comportement existant, et avoir un `npm run build` + `npm test` verts.

## Fichiers concernés

- `lib/server/tournaments/index.ts` (corriger `any[]`, optionnel : déplacer le code métier)
- `app/(secured)/tournois/[id]/page.tsx` (alléger)
- `app/(secured)/tournois/[id]/_components/BracketTree.tsx` (**nouveau**)
- `app/(secured)/tournois/[id]/_hooks/useScoreForm.ts` (à utiliser ou supprimer)
- `app/(secured)/tournois/[id]/_hooks/useKeyboardShortcuts.ts` (à utiliser ou supprimer)
- `app/(secured)/tournois/[id]/_components/ScoreInputDialog.tsx` (clarifier le rôle)

## Hors périmètre

- Ne pas créer `AdminPanel`, `TournamentHeader`, `BracketView` comme dans le plan original — l'extraction de `BracketTree` + l'usage des hooks suffit à descendre la page sous la barre cible. Garder le scope minimal.
- Ne pas re-splitter `lib/server/tournaments/index.ts` plus que nécessaire. Si tu juges l'extraction risquée (>1h), **laisse en l'état** mais corrige au moins le `any`.

## Implémentation

### A. Corriger le lint dans `tournaments/index.ts`

À la ligne 209 :

```ts
const [rows] = await db.execute<any[]>(
```

→ remplacer par :

```ts
const [rows] = await db.execute<(RowDataPacket & { /* … champs explicites … */ })[]>(
```

Liste les champs réellement utilisés en dessous du `SELECT` (`id`, `name`, `description`, `format`, `game`, `max_teams`, `state`, `start_visibility_at`, `registration_open_at`, `registration_close_at`, `start_at`, `bracket_size`, `created_at`, `organizer_user_id`, `finished_at`, `has_third_place_match`, `registered_teams`). Type strict, pas de `any`.

`RowDataPacket` est déjà importé en haut du fichier.

### B. Optionnel — déplacer le code métier de `index.ts`

Si tu as du temps :
- Déplacer `syncVisibleTournaments` dans `lib/server/tournaments/state.ts`.
- Déplacer `listTournamentBuckets`, `createTournament`, `getTournamentDetail` dans un nouveau `lib/server/tournaments/public-api.ts`.
- `index.ts` ne contient plus que des `export { … } from "./…"`.

Si tu ne le fais pas, **note-le dans le résumé final** comme dette restante.

### C. Extraire `BracketTree` dans son propre fichier

1. Créer `app/(secured)/tournois/[id]/_components/BracketTree.tsx`.
2. Y déplacer **tel quel** :
   - Les constantes `SLOT_H`, `CARD_W`, `CONN_W`, `BORDER` (lignes 23–26 de `page.tsx`).
   - L'interface `BracketTreeProps` (lignes 28–38).
   - La fonction `BracketTree` (lignes 40–220).
   - Le type `MatchScoreDraft` (ligne 14) — exporté pour réutilisation.
3. Le composant doit être `"use client"` en tête.
4. Imports à conserver : `CSSProperties`, `FormEvent`, `Fragment`, types `BracketMatch`, `BracketType`, et `MatchRow`.
5. Dans `page.tsx`, importer `BracketTree` depuis `./_components/BracketTree` et **supprimer** les ~180 lignes correspondantes.

### D. Utiliser `useScoreForm` (ou le supprimer)

Inspecte [_hooks/useScoreForm.ts](../../app/(secured)/tournois/[id]/_hooks/useScoreForm.ts) :
- **S'il modélise un draft par matchId + submit** : remplacer dans `page.tsx` la gestion manuelle de `drafts` + `handleScoreChange` + `submitScore` par ce hook.
- **S'il est mal conçu ou non aligné avec le besoin (saisie score sur N matches en parallèle)** : **supprimer** le fichier et noter dans le résumé.

Critère de décision : si le hook ne sait gérer qu'un seul match à la fois, il ne couvre pas le besoin du BracketTree (saisie sur plusieurs matches simultanés) → suppression légitime.

### E. Utiliser `useKeyboardShortcuts` (ou le supprimer)

Inspecte le hook. Si la page d'origine avait des raccourcis (Enter/Escape sur les dialogs) :
- Le brancher dans `ScoreInputDialog` (Escape pour fermer, Enter pour confirmer).
Sinon : **supprimer** le fichier.

### F. Clarifier `ScoreInputDialog`

Lire le composant. Deux cas :
- **C'est un dialog admin de résolution** : renommer le state `selectedMatchForAdmin` → `selectedMatchForAdmin` (OK), et renommer le composant en `AdminScoreResolveDialog.tsx` pour lever l'ambiguïté.
- **C'est un dialog de saisie joueur** : on a un bug, car aucune ouverture de ce dialog côté joueur n'est câblée. Brancher proprement.

Décision : regarder le code du composant et adapter. Si le composant fait clairement les deux, le splitter en deux fichiers est trop intrusif → laisser tel quel mais **renommer le state local** pour qu'il reflète l'usage réel.

### G. Vérification finale

```bash
npm run lint   # doit être vert
npm run build  # doit être vert
npm test       # 247 suites doivent rester vertes
wc -l app/(secured)/tournois/[id]/page.tsx   # cible < 280 lignes
```

Lancer `npm run dev` et tester manuellement :
- Page tournoi RUNNING → l'arbre s'affiche correctement.
- Saisie de score sur un match → toast succès, draft réinitialisé.
- Bouton admin de résolution (si présent) → dialog s'ouvre.

## Acceptation

- [ ] `npm run lint` clean (aucun `any`).
- [ ] `npm run build` clean.
- [ ] `npm test` 247/247 vert.
- [ ] `page.tsx` < 280 lignes (idéal < 200).
- [ ] `BracketTree.tsx` créé et importé proprement.
- [ ] Aucun hook orphelin (`useScoreForm`, `useKeyboardShortcuts`) : soit utilisé, soit supprimé.
- [ ] Aucune régression visuelle ni fonctionnelle (arbre tournoi, scores, admin).

## Discipline

- **Pas de hook orphelin.** Un fichier hook qui n'est jamais importé est de la dette : utilise-le ou supprime-le.
- **Pas de god-index.** `index.ts` d'un sous-dossier doit être un point d'entrée maigre. Si tu n'as pas le temps de splitter, **documente** la dette restante.
- **Refactor minimal-suffisant.** L'extraction de `BracketTree` seule devrait suffire à descendre la page sous la cible — n'invente pas d'autres composants tant que ce n'est pas atteint.
- **Pas de changement de comportement.** Tu déplaces du code, tu n'en réécris pas. Si tu vois un bug évident en déplaçant, **note-le dans le résumé final**.
